import { ElementRef, EventEmitter, Renderer } from '@angular/core';
import { ImageResult, ResizeOptions } from './interfaces';
export declare class ImageUploadDirective {
    private _elementref;
    private _renderer;
    imageSelected: EventEmitter<ImageResult>;
    resizeOptions: ResizeOptions;
    private _allowedExtensions;
    allowedExtensions: string[];
    constructor(_elementref: ElementRef, _renderer: Renderer);
    readFiles(event: any): void;
    private resize(result);
    private fileToDataURL(file, result);
}
